---
name: api-doc-generator
description: >
  按照中文模板生成后端 API 开发文档。
  Use when user asks to "生成API文档", "生成接口文档", "开发文档", "后端文档", "api docs",
  "API documentation", or wants to document API endpoints before coding.
  Also triggered when user describes a new feature and says "先出文档" or "先写文档".
argument-hint: "[功能描述或需求说明]"
model: sonnet
---
# API 文档生成器

为 Java/Spring Boot 后端项目，按照中文模板格式生成 API 开发文档。

## 核心原则

**Document what exists, not what's planned.** 文档反映真实代码实现，不是理想设计。
生成前必须读取实际源码（Controller、Service、Entity），不能凭记忆或猜测填写。

## 工作流程

### 1. 读取模板和代码

1. **先读模板**：`templates/api-doc-template.md`
   - 模板中 `>` 开头的行是给AI的指令（提示词），**必须理解并遵守其要求**，但**不能将这些 `>` 行复制到生成的文档中，要进行清除。**
   - 理解两大模块结构：第一模块（接口描述）+ 第二模块（后端开发相关描述）
2. **再读代码**：先用 SearchCodebase / Grep 搜索用户提到的功能关键词（如"积分商品""批量上下架"等）定位 Controller，再读取对应的 Controller、Service、ServiceImpl、Entity 源文件，确认项目分层结构（controller/service/mapper/entity 包路径）。

### 2. 生成文档

- **强制要求**：生成的文档必须包含模板中的全部章节，不得遗漏、不得增删：

  # 第一模块：接口描述

  1. 接口概述
  2. 请求参数（含 2.1 请求体参数、2.2 请求字段说明）
  3. 响应格式（含 3.1 响应字段说明）
  4. 枚举定义（如适用，无枚举则省略此节）

  # 第二模块：后端开发相关描述

  1. 控制层与Service实现（含 1.1 控制层代码位置、1.2 控制层代码示例、1.3 Java Service 实现）
  2. 查询相关说明（2.1 / 2.2，不适用的小节填"无"；2.2 每条 SQL 用 2.2.x 小标题）
  3. 关键实现说明（非纯 CRUD 时说明使用了哪些特殊组件/方法；纯 CRUD 填"无"）
  4. 执行流程（4.1 Mermaid 流程图 + 4.2 执行步骤说明）
  5. 数据库相关说明（5.1 + 5.2 填已存在的表，5.3 仅填需要新建的表；全存在则无 5.3，全不存在则 5.1/5.2 空只填 5.3）

- **章节标题必须与模板完全一致**（包括"第一模块""第二模块"、"1. 2. 3. 4. 5."的编号格式）
- 每个章节下的内容必须根据实际代码填充，不能留空（"枚举定义"如无枚举可省略）
- 代码块使用正确语法高亮（json、java、xml、mermaid）
- 表格展示参数，对齐清晰

### 2.1 输出文件

- 文件名用中文功能描述，如 `积分商品批量上下架接口文档.md`
- **必须使用Write工具创建文件**，不能只输出内容
- 写入目录：`C:\Users\86158\OneDrive\obsidian-vaults\xianganCompany开发仓库`
- 完整路径示例：`C:\Users\86158\OneDrive\obsidian-vaults\xianganCompany开发仓库\商户端附件分页列表接口文档.md`

### 2.2 生成后自检（强制执行）

生成文档后，必须逐项检查：

- [ ] 文档是否包含第一模块（接口描述）的全部章节？
- [ ] 文档是否包含第二模块（后端开发相关描述）的全部章节？
- [ ] 章节标题是否与模板一致（第一模块/第二模块、"1. 2. 3. 4. 5."编号）？
- [ ] 所有 `>` 提示词行是否已遵守其要求，且**未出现在生成文档中**？
- [ ] 请求参数表格（2.2）是否包含：字段、类型、必填、说明？
- [ ] 响应字段表格（3.1）是否包含：字段、类型、说明？
- [ ] 是否包含JSON格式的请求体（2.1）和响应格式（3）示例？
- [ ] 代码示例是否包含Controller（1.2）和Service（1.3）两段代码？
- [ ] 查询相关说明：2.1 / 2.2 是否按实际情况填写？不适用的小节是否填了"无"？
- [ ] 关键实现说明（3）：非纯 CRUD 时是否说明了特殊组件/方法？纯 CRUD 是否填了"无"？
- [ ] 执行流程（4）：是否包含 Mermaid 流程图 + 执行步骤说明？
- [ ] 数据库：已存在的表是否在 5.1 + 5.2 中列出？（字段从 Entity 反推）
- [ ] 数据库：需要新建的表是否在 5.3 给出了建表 DDL？（全存在则无此节）

**如有任何一项不符合，必须重新生成。**

### 3. 交付前自检

- 功能覆盖：是否覆盖了用户提到的所有接口
- 格式正确：是否严格遵循模板结构，`>` 提示词行是否已遵守，且未出现在文档中
- 代码准确：Controller/Service 路径、方法名、参数是否与源码一致
- 表结构完整：字段是否从实体类反推，与数据库对应

### 4. 等待审核

提交后提醒用户：

1. 请审核文档是否符合需求
2. 有修改要求请告知
3. **审核通过后**再开始写代码
